#include<stdio.h>
#include<stdlib.h>

struct node
{
    int data;
    struct node * next;
};

void input ()
{
    int *ptr;
    int i=0;
    while(i != NULL)
    {
    struct node * head = (struct node *)malloc(sizeof(struct node));    
    printf("Enter the number %d element of list : %d",i,);
    scanf("%d",&a);
    head->data=a;
    head->next=
    }
}
int main()
{
    
    return 0;
}